:root {
  --bg: #0c0c0f;
  --card: #121219;
  --text: #e6e6f0;
  --muted: #a9acc3;

  /* Accent gradient */
  --g1: #8a5cff;
  --g2: #23c7d9;
  --g3: #ff7b8a;

  --bubble-user: #2b2e45;
  --bubble-bot: linear-gradient(180deg, rgba(138,92,255,.22), rgba(35,199,217,.18));
  --ring: rgba(138,92,255,.45);

  --shadow: 0 10px 30px rgba(0,0,0,.35);
}

:root[data-theme="light"] {
  --bg: #f5f7fb;
  --card: #ffffff;
  --text: #1e2030;
  --muted: #586079;
  --bubble-user: #e9ecff;
  --bubble-bot: linear-gradient(180deg, rgba(138,92,255,.13), rgba(35,199,217,.10));
  --ring: rgba(35,199,217,.45);
  --shadow: 0 10px 30px rgba(12, 23, 45, .12);
}

/* Global */
* { box-sizing: border-box; }
html, body { height: 100%; }
body {
  margin: 0;
  font-family: Inter, system-ui, -apple-system, Segoe UI, Roboto, "Helvetica Neue", Arial, "Noto Sans", "Apple Color Emoji", "Segoe UI Emoji", "Segoe UI Symbol", "Noto Color Emoji";
  background: radial-gradient(1200px 800px at 10% -10%, #151523 0%, var(--bg) 60%),
              radial-gradient(1200px 800px at 110% 110%, #0f1420 0%, var(--bg) 60%);
  color: var(--text);
  line-height: 1.45;
}

/* Floating color orbs */
.bg-orb {
  position: fixed;
  filter: blur(60px);
  opacity: .35;
  pointer-events: none;
  z-index: 0;
}
.orb-1 { width: 420px; height: 420px; top: -80px; left: -80px; background: radial-gradient(circle, var(--g1), transparent 60%); }
.orb-2 { width: 380px; height: 380px; bottom: -80px; right: -40px; background: radial-gradient(circle, var(--g2), transparent 60%); }
.orb-3 { width: 360px; height: 360px; top: 40%; left: 70%; background: radial-gradient(circle, var(--g3), transparent 60%); }

/* Shell */
.chat-shell {
  position: relative;
  z-index: 1;
  max-width: 980px;
  margin: 24px auto;
  padding: 20px;
}

.chat-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  background: var(--card);
  border: 1px solid rgba(255,255,255,.06);
  border-radius: 20px;
  padding: 16px 18px;
  box-shadow: var(--shadow);
  backdrop-filter: blur(6px);
}

.brand {
  display: flex;
  gap: 14px;
  align-items: center;
}
.brand h1 {
  margin: 0;
  font-size: 20px;
  font-weight: 800;
  letter-spacing: .2px;
}
.subtitle {
  margin: 0;
  font-size: 12px;
  color: var(--muted);
}

.logo-sparkle {
  width: 40px; height: 40px;
  display: grid; place-items: center;
  background: linear-gradient(135deg, var(--g1), var(--g2));
  color: white;
  border-radius: 12px;
  box-shadow: 0 10px 30px rgba(138,92,255,.35), 0 6px 18px rgba(35,199,217,.25);
}

.ghost-btn {
  background: transparent;
  border: 1px solid rgba(255,255,255,.12);
  color: var(--text);
  padding: 8px 12px;
  border-radius: 12px;
  cursor: pointer;
  transition: transform .08s ease, border-color .2s ease;
}
.ghost-btn:hover { transform: translateY(-1px); border-color: var(--ring); }

/* Chat window */
.chat-window {
  margin-top: 16px;
  background: var(--card);
  border: 1px solid rgba(255,255,255,.06);
  border-radius: 22px;
  height: calc(100vh - 280px);
  min-height: 360px;
  overflow-y: auto;
  padding: 18px;
  box-shadow: var(--shadow);
  scroll-behavior: smooth;
}

.msg-row {
  display: grid;
  grid-template-columns: 44px 1fr;
  align-items: start;
  gap: 10px;
  margin: 12px 0;
  animation: slideIn .18s ease both;
}
@keyframes slideIn {
  from { transform: translateY(6px); opacity: 0; }
  to   { transform: translateY(0); opacity: 1; }
}

.avatar {
  width: 44px; height: 44px;
  display: grid; place-items: center;
  border-radius: 50%;
  background: linear-gradient(135deg, rgba(138,92,255,.25), rgba(35,199,217,.25));
  border: 1px solid rgba(255,255,255,.08);
}

.bubble {
  padding: 12px 14px;
  border-radius: 16px;
  border: 1px solid rgba(255,255,255,.08);
  background: var(--bubble-bot);
  box-shadow: 0 8px 24px rgba(0,0,0,.12);
  white-space: pre-wrap;
  word-wrap: break-word;
}

.msg-row.user {
  grid-template-columns: 1fr 44px;
}
.msg-row.user .avatar { order: 2; background: linear-gradient(135deg, rgba(255,123,138,.35), rgba(35,199,217,.25)); }
.msg-row.user .bubble {
  order: 1;
  background: var(--bubble-user);
}

/* Typing dots */
.typing .bubble { display: inline-flex; gap: 6px; align-items: center; }
.dot {
  width: 8px; height: 8px; border-radius: 50%;
  background: currentColor; opacity: .6;
  animation: pulse 1.2s infinite ease-in-out;
}
.dot:nth-child(2) { animation-delay: .15s; }
.dot:nth-child(3) { animation-delay: .3s; }
@keyframes pulse {
  0%, 80%, 100% { transform: scale(.7); opacity: .4; }
  40% { transform: scale(1); opacity: 1; }
}

/* Composer */
.composer {
  display: grid;
  grid-template-columns: 1fr auto;
  gap: 10px;
  margin-top: 12px;
  background: var(--card);
  border: 1px solid rgba(255,255,255,.06);
  border-radius: 16px;
  padding: 12px;
  box-shadow: var(--shadow);
}

#messageInput {
  width: 100%;
  resize: none;
  border: 1px solid rgba(255,255,255,.08);
  background: linear-gradient(180deg, rgba(35,199,217,.08), rgba(138,92,255,.08));
  border-radius: 12px;
  padding: 12px 14px;
  color: var(--text);
  font: inherit;
  line-height: 1.45;
  outline: none;
  transition: border-color .2s ease, box-shadow .2s ease;
}
#messageInput:focus {
  border-color: var(--ring);
  box-shadow: 0 0 0 4px rgba(138,92,255,.15);
}

.send-btn {
  padding: 12px 16px;
  border: none;
  border-radius: 12px;
  font-weight: 700;
  color: white;
  cursor: pointer;
  background: linear-gradient(135deg, var(--g1), var(--g2), var(--g3));
  background-size: 200% 200%;
  animation: sheengrad 6s ease infinite;
  box-shadow: 0 10px 24px rgba(138,92,255,.35), 0 6px 16px rgba(35,199,217,.25);
  transition: transform .08s ease, filter .2s ease;
}
.send-btn:hover { transform: translateY(-1px); filter: brightness(1.05); }
.send-btn:disabled { opacity: .6; cursor: not-allowed; }

@keyframes sheengrad {
  0% { background-position: 0% 50%; }
  50% { background-position: 100% 50%; }
  100% { background-position: 0% 50%; }
}

.tiny {
  margin: 10px auto 0;
  max-width: 980px;
  text-align: center;
  color: var(--muted);
  font-size: 12px;
}

kbd {
  background: rgba(255,255,255,.08);
  border: 1px solid rgba(255,255,255,.12);
  border-bottom-width: 2px;
  padding: 2px 6px;
  border-radius: 6px;
  font-size: 11px;
}
